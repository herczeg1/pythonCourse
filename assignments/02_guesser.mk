Write a function that will challenge an user to guess a random number between a lof and a high number in a given amount of attempts.
The function accepts the following arguments:

how_many_tries: the max number of attempts give to the users, defaults to three if not provided
low: lower end of the range in which the number is chosen, defaults to 1
high: higher end of the range in which the number is chosen, defaults to 10

Possible signature:
guess_the_number(how_many_tries=3, low=1, high=10)


Use
- while
- random
